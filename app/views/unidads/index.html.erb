<p id="notice"><%= notice %></p>

<h1>Listing Unidads</h1>

<table>
  <thead>
    <tr>
      <th>Descrip</th>
      <th>Valorconversion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @unidads.each do |unidad| %>
      <tr>
        <td><%= unidad.descrip %></td>
        <td><%= unidad.valorconversion %></td>
        <td><%= link_to 'Show', unidad %></td>
        <td><%= link_to 'Edit', edit_unidad_path(unidad) %></td>
        <td><%= link_to 'Destroy', unidad, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Unidad', new_unidad_path %>
