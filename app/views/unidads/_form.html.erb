<%= form_for(@unidad) do |f| %>
  <% if @unidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unidad.errors.count, "error") %> prohibited this unidad from being saved:</h2>

      <ul>
      <% @unidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descrip %><br>
    <%= f.text_field :descrip %>
  </div>
  <div class="field">
    <%= f.label :valorconversion %><br>
    <%= f.text_field :valorconversion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
