CLEAR

SET DATE BRITISH
SET CENTURY ON
SET EXCLUSIVE OFF
SET DELETED ON
SET PROCEDURE TO LIBRERIA

*
CLOSE DATABASES ALL
USE T:\PVENTA\MA05 ALIAS MA05 
SET FILTER TO TIPO == "2"
GO TOP 
DO WHILE !EOF()
	cRuc = ALLTRIM(RUC_CLI)
	IF ValidaRuc(cRuc) == .F.
		?TIPO,COD_CLI,RUC_CLI
		DELETE IN MA05
	ELSE
		IF LEN(ALLTRIM(NOM_CLI)) < 5
			DELETE IN MA05
		ENDIF 	
	ENDIF 		

	SKIP 

ENDDO 

CLOSE DATABASES all
RETURN 